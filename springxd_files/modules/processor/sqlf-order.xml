<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-jdbc="http://www.springframework.org/schema/integration/jdbc"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/jdbc http://www.springframework.org/schema/integration/jdbc/spring-integration-jdbc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<int:channel id="input" />
	
	<!-- Inject SQL from XD DSL and use SPEL to parse JSON payload -->
	<int-jdbc:outbound-channel-adapter
		channel="input" data-source="dataSource" sql-parameter-source-factory="spelSource" query="${sql}">
	</int-jdbc:outbound-channel-adapter>
	
	<!-- Table to JSON Mapping -->
	<bean id="spelSource"
		class="org.springframework.integration.jdbc.ExpressionEvaluatingSqlParameterSourceFactory">
		<property name="parameterExpressions">
			<map>
				<entry key="CUSTOMER_ID" value="payload.get('customerId').asInt()" />
				<entry key="ORDER_ID" value="payload.get('orderId').asInt()" />
				<entry key="ORDER_AMOUNT" value="payload.get('orderAmount').asDouble()" />
				<entry key="STORE_ID" value="payload.get('storeId').toString()"  />
				<entry key="ITEMS" value="payload.get('numItems').asInt()"/>
			</map>
		</property>
	</bean>
	
	<!-- Datasource mapping -->
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="com.vmware.sqlfire.jdbc.ClientDriver" />
		<property name="url" value="jdbc:sqlfire://${url:sqlfire}:7711" />
		<property name="username" value="${username:APP}" />
		<property name="password" value="${password:APP}" />
	</bean>

</beans>